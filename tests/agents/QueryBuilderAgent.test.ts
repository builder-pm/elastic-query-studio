import { QueryBuilderAgent } from '../../src/background/agents/QueryBuilderAgent';
// import { LLMClient } from '../../src/background/services/LLMClient'; // If needed

describe('QueryBuilderAgent', () => {
  it.todo('should build a simple query');
});
